import { HellodirectDirective } from './hellodirect.directive';

describe('HellodirectDirective', () => {
  it('should create an instance', () => {
    const directive = new HellodirectDirective();
    expect(directive).toBeTruthy();
  });
});
